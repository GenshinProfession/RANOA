# 阶段 0 验收记录：Fork 基线与隔离开发环境

验收日期：2026-08-18

## 1. 基线信息

| 项目 | 结果 |
| --- | --- |
| 开发分支 | `codex/harness-plan` |
| 基线提交 | `2a6e53710f6409e0cceb3de839a62f8cdf3ca3ca` |
| Fork | `https://github.com/GenshinProfession/pi-web.git` |
| Upstream | `https://github.com/agegr/pi-web.git` |
| 仓库 Pi Web | `0.8.9` |
| 仓库 Pi Coding Agent | `0.84.2` |
| 本机全局 Pi Web | `0.8.7` |

`origin/main` 与 `upstream/main` 在建基线时均指向上述提交。

## 2. 隔离验收

| 验收项 | 标准 | 实测结果 | 状态 |
| --- | --- | --- | --- |
| 端口隔离 | Harness 不使用现有 Pi Web 的 `30141` | 默认使用 `31141`；启动器拒绝 `30141` | 通过 |
| 数据隔离 | Harness 不读写 `~/.pi/agent` | 默认使用 `<repo>/.pi-harness-dev/agent`；启动器拒绝现有目录及其父、子目录 | 通过 |
| 页面启动 | Harness 首页可访问 | `GET /` 返回 `200` | 通过 |
| 会话隔离 | 新实例不加载现有会话 | `GET /api/sessions` 返回空会话集合 | 通过 |
| 旧数据不变 | 启停 Harness 前后现有数据元信息一致 | 文件数均为 `8888`，元信息指纹均为 `6725BD224CE91A0EA8296CA980F5AF861FB4F353F9FC676BEF8EB85AD72D1AD6` | 通过 |
| 停止清理 | 停止后无遗留监听 | `30141`、`31141` 均未监听 | 通过 |

当前阶段不执行旧数据复制、转换或迁移。迁移必须在后续阶段包含备份、只读扫描、试迁移、数量校验、抽样回放和回滚步骤。

## 3. 自动化检查

| 检查 | 结果 | 状态 |
| --- | --- | --- |
| `npm ci` | 安装成功，审计结果 `0 vulnerabilities` | 通过 |
| `node_modules/.bin/tsc --noEmit` | 无类型错误 | 通过 |
| `npm run lint` | 无错误、无警告 | 通过 |
| `node --test scripts/dev-harness.test.mjs` | `5/5` 通过 | 通过 |
| Next 配置定向检查 | 隔离测试与配置测试合计 `8/8` 通过；实际启动未改写 `AGENTS.md` | 通过 |
| `npm test` | `593` 项：`590` 通过、`0` 失败、`3` 跳过 | 通过 |
| `npm run build` | 生产构建、类型检查、14 个静态页面生成均成功 | 通过 |

原先的 35 个失败已全部解决，根因分为三类：Windows 下 Jiti 对相对路径和 TypeScript 路径别名产生两份 i18n 上下文；源码结构测试未统一 CRLF/LF；目录链接和 PATH 期望依赖宿主平台。修复只调整测试装配与跨平台断言，没有弱化生产代码的 `I18nProvider` 约束。隔离启动器新增的 5 项测试全部通过。

## 4. 真实模型链路验收

测试账号：本机 Pi 中用户明确授权的“基元律动”配置。测试过程只读原配置，临时将单个供应商写入隔离目录，结束后立即清空。

| 验收项 | 实测结果 | 状态 |
| --- | --- | --- |
| 供应商连通性 | `deepseek-v4-pro-0813` 返回 HTTP `200`；`deepseek-v4-flash-0731` 返回供应商侧 `503` | Pro 通过，Flash 当前不可用 |
| 创建会话并发送消息 | 在隔离目录创建真实 Pi 会话并完成模型响应 | 通过 |
| 工具调用 | 模型调用 `read` 读取仓库 `package.json`，上下文中有 2 条工具调用/结果证据 | 通过 |
| SSE 实时事件 | 收到 connected、agent、message、tool execution、settled 和 prompt_done 完整事件链 | 通过 |
| 断线恢复 | 在 `agent_start` 后主动断开，并在模型仍运行时重新连接成功 | 通过 |
| 响应校验 | 最终回复为 `@agegr/pi-web` | 通过 |
| Token 用量 | input `1137`、output `7`、cache read `9856`、cache write `0` | 通过 |
| 凭据清理 | 隔离配置剩余供应商 `0`，工作区 Token 精确匹配数 `0` | 通过 |

本次消息中的 cost 字段全部为 `0`，原因是该自定义供应商模型没有配置价格，而不是调用免费。它验证了后续计费中心必须保存供应商真实计量、价格版本和数据来源，不能把 Pi 当前的本地 cost 字段直接视为最终账单。

## 5. 验收结论

隔离开发底座已完成，可以保证后续二开默认不会与本机现有 Pi Web 的端口和数据目录冲突；旧数据保持原位，尚未迁移。

阶段 0 已通过：全量测试、类型检查、Lint、生产构建、隔离启动、数据保护、真实模型调用、工具运行、SSE 断线恢复和 Token 用量采集均已验证。`deepseek-v4-flash-0731` 的供应商侧 `503` 作为外部模型可用性问题记录，不阻塞 Harness 基线；后续默认使用已验证的 `deepseek-v4-pro-0813` 做受控测试。
